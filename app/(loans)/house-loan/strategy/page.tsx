import { StrategyTimeline } from "@/components/loans/strategy-timeline";

export const metadata = { title: "Strategy — Casa Loans" };

export default function StrategyPage() {
  return <StrategyTimeline />;
}
